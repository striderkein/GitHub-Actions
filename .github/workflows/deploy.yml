name: AWS S3 Sync

on:
  push:
    branches:
      - staging
  pull_request:
    branches:
      - staging

jobs:
  publish:
    runs-on: ubuntu-latest
    #strategy:
    #  # python-version内のPyPyのバージョンが利用できる。 たとえばpypy2とpypy3
    #  matrix:
    #    # python-version: [2.7, 3.5, 3.6, 3.7, 3.8]
    #    python-version: [3.8]
    steps:
      - name: Setup Python default encoding
        run: python -c "import sys; reload(sys); sys.setdefaultencoding('utf-8')"
      - name: Checkout
        uses: actions/checkout@v1
        with:
          fetch-depth: 1

      - name: Publish to AWS S3
        # uses: opspresso/action-s3-sync@master
        # env:
        #   AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
        #   AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        #   AWS_REGION: “ap-northeast-1”
        #   FROM_PATH: “./target/publish”
        #   DEST_PATH: “s3://test-github-actions-20200325/”
        #   # OPTIONS: “--acl public-read”
        # uses: jakejarvis/s3-sync-action@master
        # with:
        #   args: --acl public-read --follow-symlinks --delete
        #env:
        #  AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
        #  AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        #  AWS_REGION: 'ap-northeast-1'   # optional: defaults to us-east-1
        #  SOURCE_DIR: './target/publish'      # optional: defaults to entire repository
        #  # AWS_S3_BUCKET: ${{ secrets.AWS_S3_BUCKET }}
        #  AWS_S3_BUCKET: “test-github-actions-20200325/”
        # uses: actions/aws/cli@master # <- now deleted. cf. https://github.com/aws/aws-cli/issues/4835
        # comment
        uses: ItsKarma/aws-cli@v1.70.0
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        with:
          args: s3 sync --acl public-read --delete ./target/publish s3://test-github-actions-20200325
